name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Setup CocoaPods
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Install dependencies
        run: npm ci

      - name: Prebuild (Generate Native Code)
        run: npx expo prebuild --platform ios --clean

      - name: Install Pods
        run: |
          cd ios
          pod install
        
      - name: Build iOS App (Unsigned)
        run: |
          # Initial build to generate the .app
          # We check the name of the workspace - listing logic handles "IPTV Player" vs "IPTVPlayer"
          cd ios
          WORKSPACE=$(find . -name "*.xcworkspace" | head -n 1)
          
          # More robust scheme detection
          if [ -z "$WORKSPACE" ]; then
             echo "Error: No workspace found!"
             exit 1
          fi

          echo "Found workspace: $WORKSPACE"
          
          # List all schemes for debugging
          xcodebuild -workspace "$WORKSPACE" -list
          
          # Attempt to find the main scheme (usually matches the sanitized app name)
          # We filter out Pods and other auxiliary schemes if possible, but usually the first scheme is the app.
          SCHEME=$(xcodebuild -workspace "$WORKSPACE" -list | sed -n '/Schemes:/,$p' | tail -n +2 | head -n 1 | xargs)
          
          if [ -z "$SCHEME" ]; then
             echo "Error: Could not detect scheme!"
             exit 1
          fi
          
          echo "Building Workspace: $WORKSPACE with Scheme: $SCHEME"
          
          xcodebuild -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO

      - name: Package IPA
        run: |
          # Debugging: List directory structure to help diagnose missing .app
          echo "DEBUG: Listing ios/build structure:"
          find ios/build -maxdepth 5 || echo "ios/build not found"
          
          mkdir -p Payload
          
          # Find the .app in the derived data path (recursive search)
          # We use -type d because .app is a directory
          APP_PATH=$(find ios/build -name "*.app" -type d | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "Error: .app not found in ios/build!"
            exit 1
          fi
          
          echo "Found .app at: $APP_PATH"
          cp -r "$APP_PATH" Payload/
          
          # Zip it into an IPA
          zip -r iptv-player.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: iptv-player.ipa
